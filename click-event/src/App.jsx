import Button from "./Button"

function App() {
  return(
    <button>Click me</button>
  );
}

export default App

// click event - An interaction when a user clicks on a specific element. 
// We can respond to click by paasing a callback to onClick event handler